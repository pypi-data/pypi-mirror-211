[tool.poetry]
name = "py-mysql-distill"
version = "0.1.2"
description = ""
authors = ["Benjamin J Doherty <bjd@pobox.com>"]
readme = "README.md"
packages = [{include = "mysql_distill"}]

[tool.poetry.dependencies]
python = "^3.10"

[tool.poetry.group.dev.dependencies]
black = "^23.3.0"
coverage = "^7.2.6"
mypy = "^1.3.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.black]
target-version = ['py310']
