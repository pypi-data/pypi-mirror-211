from setuptools import setup

setup(
    name='TheMathModule',
    version='0.2.0',
    author='Syed Abdullah Saad',
    author_email='syedabdullahsaad2000@gmail.com',
    description='A basic module that takes the dataframe as the first argument and a numeric column of the dataframe, and calculates the mean of it (alias it as aom),///from TheMathModule.avg_or_mean import avg_or_mean as aom///'
,
    packages=['TheMathModule'],
)
