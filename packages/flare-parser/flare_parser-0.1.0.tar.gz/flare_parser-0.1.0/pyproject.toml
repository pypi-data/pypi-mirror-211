[tool.poetry]
name = "flare-parser"
version = "0.1.0"
description = ""
authors = ["user <user@example.com>"]
readme = "README.md"
packages = [{include = "flare_parser"}]

[tool.poetry.dependencies]
python = "^3.9"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
