[build-system]
requires = ["setuptools>=61.0", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "mapi_packaging_tutorial"
version = "1.2.3"
requires-python = ">=3.8,<3.11"

dependencies = [
    "numpy>=1.23"
]

