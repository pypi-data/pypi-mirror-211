apiVersion: cloudfront.aws.infra/v1alpha1
kind: ContinuousDeploymentPolicy
pipelineConfig:
  no-apply: true
  comment: 'no-apply: true prevents this file being apply by api-to-yaml. auto added
    by api-to-yaml export. if need to apply, set to false'
metadata:
  Id: 13966477-f515-47cd-a45e-937ba0cfa8a1
  Enabled: true
spec:
  StagingDistributionDnsNames:
    - $file(Distribution/www.qa1fdg.net.staging.yaml, DomainName)
  TrafficConfig:
    Type: SingleHeader
    SingleHeaderConfig:
      Value: 'true'
      Header: aws-cf-cd-gray-env
