from setuptools import setup

import api_limit

setup(
    name='api_limit',
    version="v0.0.1",
    description='API rate limit decorator',
    long_description="",
    packages=['api_limit'],
    install_requires=[],
)
