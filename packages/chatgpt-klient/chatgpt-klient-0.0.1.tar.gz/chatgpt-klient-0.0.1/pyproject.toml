[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "chatgpt-klient"
version = "0.0.1"
dependencies = [
    "openai",
    "logutils-serpucga",
    "requests",
    "rich",
    "tiktoken"
]
